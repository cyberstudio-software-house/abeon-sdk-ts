name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout abeon-shared
        uses: actions/checkout@v4
        with:
          path: abeon-shared

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: abeon-shared/package-lock.json

      - name: Install
        working-directory: abeon-shared
        run: npm ci

      - name: Lint
        working-directory: abeon-shared
        run: npm run lint

      - name: Typecheck
        working-directory: abeon-shared
        run: npm run typecheck

      - name: Test
        working-directory: abeon-shared
        run: npm test

      - name: Build
        working-directory: abeon-shared
        run: npm run build
